# packurl: https://raw.githubusercontent.com/egiazaranmarik303-crypto/heroku---modules/refs/heads/main/testYAMAL

from .. import loader, utils

@loader.tds
class LanGuy(loader.Module):
    """Тестовый модуль для проверки бага translations"""
    strings = {
        "name": "LanGuy",
        "hello": "Hi! (fallback)"
    }

    @loader.command()
    async def hello(self, message):
        """Тест"""
        await utils.answer(message, self.strings["hello"])
