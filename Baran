# meta developer: @your_username

from .. import loader, utils


@loader.tds
class BaranMod(loader.Module):
    """Модуль для команды .baran"""

    strings = {"name": "Baran"}

    @loader.command()
    async def baran(self, message):
        """Изменяет сообщение на @midga3"""
        await utils.answer(message, "@midga3")
