from .. import loader


@loader.tds
class AskGeminiMod(loader.Module):
    """Заменяет .askGemini"""

    strings = {
        "name": "AskGemini"
    }

    async def watcher(self, message):
        if message.raw_text.strip() == ".askGemini":
            await message.edit("пошёл нафиг")
